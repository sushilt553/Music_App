<h1>All Our Bands</h1>

<ul>
    <% @bands.each do |band| %>
        <li><a href="<%= band_url(band) %>"><%= band.name %></a></li>
    <% end%>
</ul>

<a href="<%= new_band_url %>">Create a new band</a>

<br>

<a href="<%= root_url %>">Home Page</a>